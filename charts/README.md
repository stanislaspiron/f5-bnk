# Install F5 BNK Helm Chart

```bash
helm repo add --username <username> --password <password> helm-bnk-gateway  'https://raw.githubusercontent.com/stanislaspiron/k3s-bnk/master/charts/'
```

# Update repository
```bash
helm repo update
helm repo list
helm search repo bnk
```
